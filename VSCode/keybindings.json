// Place your key bindings in this file to override the defaults
[
    {
    "key": "ctrl+shift+s",
    "command": "editor.action.insertSnippet",
    "args": { "name": "section_title" }
    },
    
    {
    "key": "ctrl+alt+c",
    "command": "editor.action.insertSnippet",
    "args": { "name": "checkbox" }
    },

    {
        "key": "ctrl+alt+m",
        "command": "workbench.action.toggleMaximizedPanel",
        "when": "!terminalfocus"
    },

]